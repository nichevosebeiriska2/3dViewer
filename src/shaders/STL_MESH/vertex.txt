#version 430 core
layout(location = 0) in vec3 vertexPosition;

uniform vec3 light_pos;
uniform mat4 mvp;
uniform mat4 model;
uniform vec3 my_position;
uniform vec3 model_color;

void main() {
 vec4 v = mvp * model *vec4(vertexPosition, 1.0);

 gl_ClipDistance[0] = v.x;
gl_Position = v;
}